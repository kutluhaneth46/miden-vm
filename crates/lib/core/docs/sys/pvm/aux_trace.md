
## miden::core::sys::pvm::aux_trace
| Procedure | Description |
| ----------- | ------------- |
| observe_aux_trace | Observes the PVM auxiliary trace commitment and its twelve component sigma values.<br /><br />The advice provider supplies the commitment followed by each AIR's exposed values in proof<br />order. Eight AIRs expose one sigma; EidosCompression and BytePairAnd8 expose two apiece. The<br />second value of each composite uses Miden's normalized `sigma_prime` convention, so the<br />verifier locates those two values from the stable height order and weights them by that AIR's<br />trace length before closing the external LogUp identity.<br /><br />The stream is 28 base felts: the four-felt commitment followed by twenty-four sigma<br />coordinates. Three Eidos blocks are compressed and the final four felts remain buffered,<br />matching the lifted-STARK transcript before it samples composition challenges.<br /><br />The hook also adds the verifier-side consumes for the fixed uint/curve environment and asserts<br />`fixed_boundary_correction + sum(sigma_i) == 0` coordinate-wise.<br /><br />Precondition:  input_len=0 (the main commitment was flushed before this hook).<br />Postcondition: input_len=4, output_len=0.<br /><br />Input:  [...]<br />Output: [...]<br /> |

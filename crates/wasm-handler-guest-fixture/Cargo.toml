[package]
name = "miden-wasm-handler-guest-fixture"
version = "0.30.0"
description = "Test fixture: a Rust guest crate with Wasm event handlers built through the SDK"
publish = false
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
rust-version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]
bench = false
doctest = false
test = false

[dependencies]
miden-event-handler-sdk = { workspace = true, features = ["panic-handler"] }

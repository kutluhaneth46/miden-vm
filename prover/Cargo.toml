[package]
name = "miden-prover"
version = "0.31.0"
description = "Miden VM prover"
documentation = "https://docs.rs/miden-prover"
readme = "README.md"
categories = ["cryptography", "emulators", "no-std"]
keywords = ["miden", "prover", "stark", "zkp"]
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
rust-version.workspace = true
edition.workspace = true

[features]
default = ["std"]
concurrent = [
    "std",
    "miden-air/concurrent",
    "miden-crypto/concurrent",
    "miden-precompiles-prover/concurrent",
    "miden-processor/concurrent",
]
std = [
    "miden-air/std",
    "miden-debug-types/std",
    "miden-precompiles-prover/std",
    "miden-processor/std",
    "thiserror/std",
]

[dependencies]
# Miden dependencies
miden-air.workspace = true
miden-core.workspace = true
miden-precompiles-prover.workspace = true
miden-processor.workspace = true
miden-crypto.workspace = true

# External dependencies
serde.workspace = true
serde-wincode.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
miden-assembly.workspace = true
miden-core = { workspace = true, features = ["testing"] }
miden-debug-types.workspace = true
miden-verifier = { workspace = true, features = ["std"] }
tokio = { workspace = true, features = ["macros", "rt"] }
